// IDs of World Championship leagues (round-robin format, fewer games).
export const WORLD_CHAMPS_IDS = new Set(['1', '2', '11']);

// IDs of international / multi-national leagues that show a country flag.
export const INTERNATIONAL_CHAMPS_IDS = new Set(['3', '4', '10']);
